# 入门教程

面向第一次使用 Garnet 的人。

## 计划内容

- `getting-started.md`：安装、编译第一个程序、认识 `package` / `import`
- `syntax.md`：变量、常量、条件、循环、函数
- `types.md`：结构体、接口、枚举、newtype / alias
- `stdlib.md`：如何导入并调用 Go 标准库
- `errors.md`：`else return` 怎么用

写教程时可以直接引用 `examples/` 下的示例，并确保每个示例都能被 `tests/pass` 覆盖到。
